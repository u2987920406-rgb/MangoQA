# FAILLES — registre de l'audit MangoQA

Référence des failles identifiées par l'audit (2026-07) et de leur traitement.
Statut : ✅ FAIT · 🟡 ouvert.

| # | Faille | Priorité | Statut | Modèle optimal | Effort |
|---|--------|----------|--------|----------------|--------|
| Q1 | Famille OOM résiduelle : 3 lectures JSONL NON bornées (`readLatestBrief`, `readMetricsTail`, `loadAll` du Retex) → lecteur partagé borné `src/jsonl.ts` (queue, caps octets + lignes, 1ʳᵉ ligne tronquée jetée) + `src/test-jsonl.ts` | P0 | ✅ FAIT | — | — |
| Q2 | `index.ts` monolithique non testable → moteur extrait dans `src/orchestrator.ts` (dédup, verrou inFlight, ordre des visages, deps injectées) ; Auditeur de Suite CÂBLÉ dans le cycle de phase (gaté `SUITE_EYE=on`, défaut off) + `src/test-orchestrator.ts` | P1 | ✅ FAIT | — | — |
| Q3 | Fail-open = fail-silent : `catch {}` muets dans les runners → tous tracent désormais `console.warn("[mango-qa] <visage>: …")` (comportement inchangé : on avale toujours ; ENOENT = état normal, reste silencieux) | P1 | ✅ FAIT | — | — |
| Q4 | Angles morts de test : `src/test-verdict.ts` (contrat red/green `buildVerdict` + `parseFirstJson`) ; `test-*.ts` et `run-*.ts` inclus dans le typecheck (`tsconfig.json` include) | P2 | ✅ FAIT | — | — |
| Q5 | Polling disjoncteur relit 4 Mo tous les 5 s → lecture incrémentale par offset (`createBusEventsReader` : curseur octets + tampon borné, rotation = reset complet, cap 4 Mo conservé) + tests dans `test-disjoncteur.ts` | P2 | ✅ FAIT | — | — |
| R1 | Double parse des fichiers dans `buildGraph` (flux-eye) : chaque fichier est passé au blob global ET re-parsé individuellement → un seul passage par fichier, symboles/machines fusionnés depuis les ASTs par fichier (`symbolsFromRoots`/`machinesFromRoots`) — liens inter-fichiers préservés | P3 | ✅ FAIT | — | — |
| R2 | `DesignFile` (design-eye/runner.ts) duplique `ProjectFile` (types.ts) : deux types identiques pour la même donnée → `DesignFile` est maintenant un alias de `ProjectFile`, signatures `inspectProjectDesign`/`runDesignEye` sur `ProjectFile` | P3 | ✅ FAIT | — | — |
| R3 | Duplication `renderFiles` (llm.ts vs flux-eye/deep.ts, caps différents) et `walk`/`walkSrc` (orchestrator.ts vs flux-eye/runner.ts, extensions/caps divergents) → module partagé `src/fs-shared.ts` (`renderFiles(files, cap, label)` + `walkTree` paramétré), les deux visages pointent dessus | P3 | ✅ FAIT | — | — |

## Défauts PRODUIT trouvés à l'usage (J1/J2, 2026-08)

Ceux-ci ne viennent pas d'une relecture de code mais d'une **exécution sur du vrai code**.
Ils touchent ce que l'outil PROMET, pas sa propreté interne.

| # | Faille | Priorité | Statut |
|---|--------|----------|--------|
| J1-a | **Audit partiel silencieux** : `renderFiles` s'arrêtait au cap et abandonnait les fichiers suivants sans trace. Mesuré sur `abyss` : 5/19 fichiers réellement lus, rapport affichant « 19 fichiers » et rendant un verdict comme s'il avait tout vu → un vrai défaut dans les 73 % non lus produisait un **Feu Vert** | **P0 — touche la promesse** | ✅ FAIT (2026-08-05) — couverture MESURÉE (`AuditCoverage`) et DÉCLARÉE au modèle (bloc prompt), au rapport (`AuditReport.coverage`) et au verdict (`QAVerdict.coverage`, additif) |
| J1-b | **Hallucination sur fichier long** : sur `Catalogue.jsx` (334 lignes, intégralement présent dans le prompt), la branche Performance a exigé d'ajouter `key={c.id}` **déjà présent ligne 308**. Le corpus J0 (`0/45 faux positifs`) ne pouvait pas le voir : il ne contient que des défauts isolés dans des fichiers de 12 à 40 lignes | P1 | 🟡 MESURABLE (2026-08-05) — famille `LONG` ajoutée au corpus (`eval/corpus-long.ts`, 4 cas appariés dont le rejeu exact du faux positif). Le défaut lui-même n'est PAS corrigé : on sait désormais le mesurer |
| J1-c | **Cap de prompt hérité, jamais mesuré** : `FILE_PAYLOAD_CAP = 24 000` limitait l'auditeur à 27 % d'un projet réel, alors que la sonde plafond montre 95 731 car. (28 930 tokens) en un seul appel | P1 | ✅ FAIT (2026-08-05) — 100 000 par défaut, réglable par `QA_FILE_PAYLOAD_CAP`. Fait APRÈS J1-a, jamais avant |
| J2-a | **Plafond caché d'undici (300 s)** : `askOllama` passait par `fetch`, qui coupe à 300 s si les en-têtes ne sont pas arrivés — et avec `stream: false`, Ollama ne les envoie qu'après calcul complet. Dormant à 24 000 car., ACTIF à 100 000 : les gros audits seraient tombés en `skip` fail-open, c'est-à-dire auraient disparu sans bruit | P1 | ✅ FAIT (2026-08-05) — `node:http`, seul délai = le nôtre (`QA_OLLAMA_TIMEOUT_MS`), explicite |
| J1-d | Intégration MangoOS : un audit local sur projet réel dure ~365 s contre un budget `MANGOQA_CLOSURE_TIMEOUT` de 60 s → chaque clôture serait marquée « non-vérifiée » (fail-open) et l'audit disparaîtrait. `surfaceVerdict` (voie 300 s) n'est appelée nulle part | P2 | 🟡 OUVERT — à trancher **côté MangoOS**, sans effet sur l'usage autonome (CLI/CI/MCP) |
| J2-b | `flux-eye/deep.ts` gardait son propre cap (20 000) et rendait un payload **sans couverture** — même classe de défaut que J1-a, sur un autre visage | P2 | ✅ FAIT (2026-08-05) — couverture mesurée, déclarée au modèle, portée dans `FluxDeepObservation.coverage` ET recopiée dans le `summary` écrit en JSON. Cap **laissé à 20 000** : ce visage n'a jamais été mesuré contre un corpus, le relever sans donnée serait refaire l'erreur en sens inverse. Réglable par `FLUX_DEEP_PAYLOAD_CAP` |
| J2-c | **Le harnais de mesure était complaisant** : sur un cas propre, `relevant() === []` était compté `correct-pass`, donc au CRÉDIT de la branche. Security affichait « 0 faux positif / 6 » pour **3 jugements réels** — dénominateur gonflé de non-événements, dans le sens flatteur. C'est J1-a déplacé dans l'instrument : confondre « n'a pas regardé » et « a regardé et n'a rien trouvé » | P1 | ✅ FAIT (2026-08-05) — catégorie `hors-perimetre` distincte, exclue du dénominateur et affichée à côté |
| J3-a | **Détection du point d'entrée par le NOM du fichier** : `/cli\.(ts\|js)$/` sur `process.argv[1]`. `npm i -g` installe un lien nommé `mangoqa` — le motif ne correspond plus. La commande se serait terminée **en silence, code 0, sans rien auditer**. Invisible en développement, fatal à l'installation, et le pire mode de panne pour un outil dont la promesse est de ne pas se taire | P1 | ✅ FAIT (2026-08-05) — comparaison d'URL de module sur le chemin réel ; valide sous `tsx`, sous `node dist/` et en binaire installé. Gelé par `test-packaging.ts` |
| J3-b | **355 Mo installés pour une CLI de 1,5 Mo** : le SDK Claude (280 Mo) est le cerveau de **repli**, les grammaires WASM (50 Mo) servent un visage que la CLI n'appelle jamais. Contredit frontalement l'argument de souveraineté du produit. Aggravé par `auditProject` important ses 3 fonctions de lecture via `orchestrator.ts`, ce qui tirait tout le graphe des visages | P1 | ✅ FAIT (2026-08-05) — lecture extraite dans `project-files.ts` (déplacement pur, `orchestrator` ré-exporte) ; lourds passés en dépendances de pair **optionnelles** + imports dynamiques + messages actionnables. **355 Mo → 1,6 Mo**, serveur MCP compris |
| J2-d | En-tête des rapports d'éval **faux sur ses propres conditions** : il n'annonçait « repli Claude COUPÉ » que si `--brain` avait été passé, donc un `QA_LOCAL_ONLY=on` posé par l'environnement produisait un rapport affirmant qu'un repli cloud était armé. Plus l'horodatage en UTC (nom de fichier ET en-tête), déjà corrigé dans `_ceiling.ts` mais pas ici | P2 | ✅ FAIT (2026-08-05) — conditions lues dans l'environnement, heure locale partout |

## Notes

- **Q1/Q5** : le pattern anti-OOM (#L70) prouvé par `readBusEvents` est désormais mutualisé dans `src/jsonl.ts` (`readJsonlTail` stateless, `readJsonlSince` incrémental). Toute nouvelle lecture de `.jsonl` doit passer par ce module.
- **Q2** : `SUITE_EYE=on` active l'Auditeur de Suite dans le cycle de phase (défaut off = comportement historique). `run-suite-eye.ts` reste utilisable en CLI ponctuel.
- **Q3** : règle gravée — un `catch` fail-open sans trace est interdit ; seul ENOENT (fichier pas encore créé = état normal) reste silencieux.
- **R1** : la fusion par-fichier (au lieu du blob concaténé) peut en théorie diverger du blob dans un cas adversarial (un fichier gravement malformé qui aurait corrompu tout le parse du blob) ; sur du code réel (top-level statements indépendants), le comportement observable est identique — prouvé par la suite `test-flux-eye.ts` (résolution de constantes réparties sur 3+ fichiers tiers, setter appelé depuis 4 fichiers distincts, redéfinition de clé, fichier cassé à côté de fichiers sains).
- **R3** : `walkTree` généralise `walk`/`walkSrc` via un callback `visit` — reproduit à l'identique la nuance historique où un fichier illisible ne consommait pas de place dans `maxFiles` (flux-eye) alors qu'un chemin non lu en comptait un (orchestrator, qui ne lit pas le contenu à ce stade).
- **Visage 2 (Observateur-Conseil)** : amorce non câblée — `src/observer.ts` (module pur, événements injectés) + `src/test-observer.ts`. Détecte des patterns de défaillance récurrents (branche/règle/projet au-dessus d'un seuil configurable) et produit des SUGGESTIONS texte pour Raf, jamais d'action. Volontairement PAS branché dans `orchestrator.ts`/`index.ts` — à câbler dans une session dédiée (source d'événements réelle à définir : Retex existant ou nouvel historique de verdicts).
