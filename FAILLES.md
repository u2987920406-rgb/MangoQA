# FAILLES — registre de l'audit MangoQA

Référence des failles identifiées par l'audit (2026-07) et de leur traitement.
Statut : ✅ FAIT · 🟡 ouvert.

| # | Faille | Priorité | Statut | Modèle optimal | Effort |
|---|--------|----------|--------|----------------|--------|
| Q1 | Famille OOM résiduelle : 3 lectures JSONL NON bornées (`readLatestBrief`, `readMetricsTail`, `loadAll` du Retex) → lecteur partagé borné `src/jsonl.ts` (queue, caps octets + lignes, 1ʳᵉ ligne tronquée jetée) + `src/test-jsonl.ts` | P0 | ✅ FAIT | — | — |
| Q2 | `index.ts` monolithique non testable → moteur extrait dans `src/orchestrator.ts` (dédup, verrou inFlight, ordre des visages, deps injectées) ; Auditeur de Suite CÂBLÉ dans le cycle de phase (gaté `SUITE_EYE=on`, défaut off) + `src/test-orchestrator.ts` | P1 | ✅ FAIT | — | — |
| Q3 | Fail-open = fail-silent : `catch {}` muets dans les runners → tous tracent désormais `console.warn("[mango-qa] <visage>: …")` (comportement inchangé : on avale toujours ; ENOENT = état normal, reste silencieux) | P1 | ✅ FAIT | — | — |
| Q4 | Angles morts de test : `src/test-verdict.ts` (contrat red/green `buildVerdict` + `parseFirstJson`) ; `test-*.ts` et `run-*.ts` inclus dans le typecheck (`tsconfig.json` include) | P2 | ✅ FAIT | — | — |
| Q5 | Polling disjoncteur relit 4 Mo tous les 5 s → lecture incrémentale par offset (`createBusEventsReader` : curseur octets + tampon borné, rotation = reset complet, cap 4 Mo conservé) + tests dans `test-disjoncteur.ts` | P2 | ✅ FAIT | — | — |
| R1 | Double parse des fichiers dans `buildGraph` (flux-eye) : chaque fichier est passé au blob global ET re-parsé individuellement → un seul passage par fichier, symboles/machines fusionnés depuis les ASTs par fichier (`symbolsFromRoots`/`machinesFromRoots`) — liens inter-fichiers préservés | P3 | ✅ FAIT | — | — |
| R2 | `DesignFile` (design-eye/runner.ts) duplique `ProjectFile` (types.ts) : deux types identiques pour la même donnée → `DesignFile` est maintenant un alias de `ProjectFile`, signatures `inspectProjectDesign`/`runDesignEye` sur `ProjectFile` | P3 | ✅ FAIT | — | — |
| R3 | Duplication `renderFiles` (llm.ts vs flux-eye/deep.ts, caps différents) et `walk`/`walkSrc` (orchestrator.ts vs flux-eye/runner.ts, extensions/caps divergents) → module partagé `src/fs-shared.ts` (`renderFiles(files, cap, label)` + `walkTree` paramétré), les deux visages pointent dessus | P3 | ✅ FAIT | — | — |

## Défauts PRODUIT trouvés à l'usage (J1/J2, 2026-08)

Ceux-ci ne viennent pas d'une relecture de code mais d'une **exécution sur du vrai code**.
Ils touchent ce que l'outil PROMET, pas sa propreté interne.

| # | Faille | Priorité | Statut |
|---|--------|----------|--------|
| J1-a | **Audit partiel silencieux** : `renderFiles` s'arrêtait au cap et abandonnait les fichiers suivants sans trace. Mesuré sur `abyss` : 5/19 fichiers réellement lus, rapport affichant « 19 fichiers » et rendant un verdict comme s'il avait tout vu → un vrai défaut dans les 73 % non lus produisait un **Feu Vert** | **P0 — touche la promesse** | ✅ FAIT (2026-08-05) — couverture MESURÉE (`AuditCoverage`) et DÉCLARÉE au modèle (bloc prompt), au rapport (`AuditReport.coverage`) et au verdict (`QAVerdict.coverage`, additif) |
| J1-b | **Hallucination sur fichier long** : sur `Catalogue.jsx` (334 lignes, intégralement présent dans le prompt), la branche Performance a exigé d'ajouter `key={c.id}` **déjà présent ligne 308**. Le corpus J0 (`0/45 faux positifs`) ne pouvait pas le voir : il ne contient que des défauts isolés dans des fichiers de 12 à 40 lignes | P1 | 🟡 MESURABLE (2026-08-05) — famille `LONG` ajoutée au corpus (`eval/corpus-long.ts`, 4 cas appariés dont le rejeu exact du faux positif). Le défaut lui-même n'est PAS corrigé : on sait désormais le mesurer |
| J1-c | **Cap de prompt hérité, jamais mesuré** : `FILE_PAYLOAD_CAP = 24 000` limitait l'auditeur à 27 % d'un projet réel, alors que la sonde plafond montre 95 731 car. (28 930 tokens) en un seul appel | P1 | ✅ FAIT (2026-08-05) — 100 000 par défaut, réglable par `QA_FILE_PAYLOAD_CAP`. Fait APRÈS J1-a, jamais avant |
| J2-a | **Plafond caché d'undici (300 s)** : `askOllama` passait par `fetch`, qui coupe à 300 s si les en-têtes ne sont pas arrivés — et avec `stream: false`, Ollama ne les envoie qu'après calcul complet. Dormant à 24 000 car., ACTIF à 100 000 : les gros audits seraient tombés en `skip` fail-open, c'est-à-dire auraient disparu sans bruit | P1 | ✅ FAIT (2026-08-05) — `node:http`, seul délai = le nôtre (`QA_OLLAMA_TIMEOUT_MS`), explicite |
| J4-a | **Un cerveau incompatible produit un FEU VERT.** Vérifié en sonde le 2026-08-05 : un modèle qui RÉPOND mais ne tient pas le contrat JSON (trop petit, non-instruct) fait tomber les 6 branches en `skip` « réponse illisible » ; `buildVerdict` ne voit alors aucun `fail` bloquant et rend **`green`**, la couverture s'affiche **« complète »** (vraie mais trompeuse : les fichiers ont bien été envoyés), et la CLI sort en **code 0**. Sur un fichier contenant un vrai défaut. En CI, ça passe. C'est J1-a d'un cran plus haut : le produit distingue « n'a pas LU » de « a lu et n'a rien trouvé », mais pas **« n'a pas JUGÉ » de « a jugé et n'a rien trouvé »** | **P0 — touche la promesse** | ✅ FAIT (2026-08-08, ADR-001 lot 2) — les DEUX volets, aucun ne remplaçant l'autre. (1) `src/preflight.ts` câblé API + CLI + MCP, validé par `parseFirstJson` (le lecteur de la PRODUCTION, pas une regex) → `CerveauInutilisableError`, aucun audit lancé, code 2. (2) chaque `skip` porte sa cause en deux familles — jugement rendu (`hors-perimetre`, `juge-sans-avis`) vs panne (`reponse-illisible`, `cerveau-injoignable`, `cause-inconnue`) ; seule une panne sur branche **bloquante** interdit le vert. Contrat figé INCHANGÉ (`verdict` reste `green`, fail-open préservé) : ce sont l'affichage (`⚪ NON VÉRIFIÉ`) et le code de sortie (`EXIT.NON_VERIFIE = 4`, non désactivable) qui cessent de certifier. `estPanne(undefined) === true` — l'optimisme n'est jamais le défaut. Mesuré : `FEU VERT`/0 → `NON VÉRIFIÉ`/4, et 5,2 s pour le savoir au lieu d'un audit entier. Gelé par `tests/unit/test-abstention.ts` (17 tests). Voir `eval/rapports/LOT2-HONNETETE.md` |
| J4-b | Aucun **prérequis** n'est déclaré à l'utilisateur : ni la version de Node, ni le fait qu'un cerveau joignable est indispensable, ni que le contrat d'entrée est l'API `/api/chat` d'Ollama (et non le format OpenAI), ni les langages réellement audités (`.ts .tsx .js .jsx .css .html .json` — un projet Python rend « aucun fichier auditable »). `docs/produit.html` vend le produit sans énoncer ce qu'il faut pour qu'il fonctionne | P1 | 🟡 OUVERT |
| L2-a | **Fuite d'état entre appels MCP** : le cap de prompt transite par `process.env.QA_FILE_PAYLOAD_CAP`. Dans une CLI, un process = un audit, donc sans conséquence ; mais le serveur MCP est **long-vivant** — un seul appel passant `cap` imposait silencieusement sa couverture réduite à TOUS les appels suivants, sans que rien ne le dise. Trouvé par Mango QA sur son propre diff (branche architecture, 2026-08-08) | P1 | ✅ **FAIT (2026-08-08, lot 3)** — d'abord borné par un `try/finally`, puis **fermé sur le fond** quand la branche architecture l'a resignalé sur le diff du lot 3 : `cap` est un champ du contrat (`AuditOptions` → `AuditContext` → `auditWithLLM`), l'environnement n'est plus qu'un repli rétrocompatible, et le `try/finally` a disparu avec sa cause. Une faille enregistrée se corrige même hors de son lot ; c'est une *fonctionnalité* qui attendrait son tour. Gelé par 2 tests dans `test-llm-coverage.ts` (`ctx.cap` prime sur l'env · sans `ctx.cap`, l'env reste le repli) |
| L4-a | **`--spec` ne lit qu'un FICHIER** : l'ADR mentionnait « `--spec <fichier>` **ou une issue** ». Récupérer une issue GitHub/GitLab demande réseau + authentification, hors périmètre du lot 4 | P3 | ✅ **FAIT (2026-08-08, lot 5)** — `--spec #42` ou une URL d'issue passent par le CLI `gh` **déjà installé et authentifié chez l'utilisateur**. Aucun code d'authentification maison : pas de jeton stocké, pas de secret en variable d'environnement, aucune surface d'attaque ajoutée à un outil dont l'argument est la confiance. Absence de `gh` déclarée avec sa piste de résolution, jamais un `ENOENT: spawn gh` brut |
| P-01 | **Un audit VIDE sortait en code 0** — donc passait pour un feu vert. Un développeur Django (persona P5) pointe Mango QA sur son projet : « aucun fichier auditable », **code 0**, alors que le fichier contient une injection SQL flagrante. Une équipe Python pouvait le brancher en CI et voir passer du vert éternellement sans qu'un seul fichier soit lu. C'est J4-a d'un cran plus haut : le produit savait dire « je n'ai pas lu » et « je n'ai pas jugé », pas « je n'ai rien eu à regarder » | **P0 — touche la promesse** | ✅ FAIT (2026-08-08) — `empty` → **code 4**, et le rapport NOMME les extensions lues (`EXTENSIONS_AUDITEES`, source unique) en disant explicitement « ce n'est pas un feu vert sur votre code, c'est une absence de lecture ». Une limite qu'on ne nomme pas se lit comme une absence de problème. 3 tests |
| P-02 | **Le détecteur de tests ne connaissait pas la convention du dépôt lui-même** : `TEST_FILE_RE` ne reconnaissait que `foo.test.ts`/`foo.spec.ts`. Mango QA nomme ses tests `test-cli.ts` → **son propre dépôt était vu comme dépourvu de tests**, et les branches recevaient le signal « aucun fichier de test n'existe nulle part », sur la foi duquel la branche Tests peut rendre un Feu Rouge. Trouvé par le produit en s'auditant lui-même (persona P7), qui a nommé la cause exacte | P1 | ✅ FAIT (2026-08-08) — préfixe ET suffixe couverts (`test-x.ts`, `test_x.tsx`, `x_test.ts`, `x-spec.js`, `x.test.ts`). Séparateur **obligatoire** : sans lui `latest.ts` et `contest.ts` passeraient pour des tests, et un faux positif ici **éteindrait un vrai Feu Rouge**. Gelé par un test à 9 cas |
| P-03 | **Le rapport machine ne portait ni date, ni cerveau, ni version.** Contradiction interne : la sortie TEXTE annonce le cerveau depuis le lot 0 (« deux audits rendus par deux cerveaux différents ne sont pas comparables »), mais la sortie MACHINE — la seule qu'on archive vraiment en CI, en livraison client ou dans une chaîne d'agents — ne le disait pas (persona P4, freelance qui veut joindre le rapport à sa facture) | P1 | ✅ FAIT (2026-08-08) — `AuditReport.conditions` (date ISO **locale avec décalage**, cerveau, modèle, version), porté par la CLI, le JSON et le schéma MCP. Au passage : `VERSION` était dupliquée dans `cli.ts` ET `mcp.ts` — **troisième duplication de règle du dépôt** — extraite dans `src/version.ts` |
| P-04 | **La CLI ne faisait pas ce que sa propre doctrine annonçait** : la décision **D1** fait de Claude le défaut PRODUIT, et `llm.ts` l'écrit noir sur blanc (« la CLI et la documentation recommandent claude ») — mais `cerveauPrimaire()` rendait `ollama` dès que `QA_BRAIN` n'était pas posé. Vécu par le persona P2 : hook installé dans son dépôt, aucun `.env`, audit parti sur le modèle local → **344 s au lieu de 20**, et un feu rouge rendu par le cerveau que le produit déconseille. Un pre-push de six minutes se fait désinstaller le jour même | P1 | ✅ FAIT (2026-08-08) — la CLI pose `QA_BRAIN=claude` quand rien ne le fixe. La **bibliothèque** garde `ollama` (byte-identique pour MangoOS). ⚠️ Exception : jamais sous `QA_LOCAL_ONLY=on`, qui promet qu'aucun octet ne sort de la machine — basculer silencieusement sur Claude y trahirait la promesse |
| P-05 | **Le hook pre-push annonçait un FEU ROUGE sur une panne d'installation.** Il enchaînait deux `npx` et prenait le code de ce qui restait : quand aucun ne pouvait s'installer, npm rendait 1 → « poussée refusée (code 1) », c'est-à-dire feu rouge, sur du code que personne n'avait lu. Il déversait en plus la sortie brute de npm. Et il supposait `origin/HEAD`, absent de beaucoup de dépôts | **P1 — une panne d'infrastructure présentée comme un verdict est ce que ce produit combat** | ✅ FAIT (2026-08-08) — l'auditeur est **trouvé avant d'être appelé** (`command -v`) ; s'il est absent : message clair et **exit 0** (fail-open, invariant n°1 — une panne de l'auditeur ne bloque jamais le travail). Référence de comparaison prise sur `@{upstream}`, repli sur le travail non commité. Vérifié en réel : `sh -n` passe, absence → 0, environnement cassé → **2**, vrai défaut → **1**. 3 tests |
| P-06 | **Le workflow de CI « prêt à coller » échoue au premier push** : il appelle `npx -y mango-qa`, or le paquet n'est pas publié (`private: true` jusqu'au lot 6). Marc (persona P3) le collerait et prendrait un 404 | P2 | ✅ **BORNÉ** (2026-08-08) — avertissement en tête du fichier généré, avec la marche à suivre en attendant. Se refermera tout seul à la publication, elle-même conditionnée par [[L142]] |
| P-07 | Messages et affichages : un registre de branches vide produisait « Aucune branche ne correspond à : **undefined** » ; la branche Spec sans spec affichait « 2 fichiers » (ce qui se lit comme « j'ai regardé 2 fichiers », sur un produit dont la thèse est justement de ne pas laisser croire ça) ; « les **1** fichiers » | P3 | ✅ FAIT (2026-08-08) — causes distinguées, `—` au lieu d'un compte trompeur, accords au singulier. Petites verrues, mais sur un produit qui vend la rigueur du propos |
| P-08 | **Date UTC affichée comme locale** — introduite puis corrigée dans la même séance : le premier jet de `conditions.date` utilisait `toISOString()` sous un commentaire disant « heure locale », d'où « rendu le 03:21 » pour un audit lancé à 05:21. **Troisième passage de ce piège dans ce dépôt** (`_ceiling.ts`, rapports d'éval) | P2 | ✅ FAIT (2026-08-08) — `dateLocaleIso()`, ISO 8601 **avec décalage** (`+02:00`), relisible par un lecteur de dates standard. Un rapport daté de deux heures avant l'audit est pire qu'un rapport sans date : il a l'air précis |
| L6-f | **La panne `Reached maximum number of turns (1)` n'est pas aléatoire** : `architecture` est tombée dessus sur **3 des 4 audits** d'`abyss` — la branche au plus gros prompt (19 fichiers). Taux global sur la journée ≈ **3 %** des appels, mais **pas uniforme** : il se concentre sur les prompts les plus longs. `askClaude` fixe `maxTurns: 1` | P1 | 🟡 OUVERT — **délibérément non corrigé ce soir** : toucher à la configuration du cerveau après avoir publié des chiffres mesurés sous cette configuration les invaliderait. Piste : relever `maxTurns`, ou découper le payload d'`architecture`. À faire AVANT toute nouvelle campagne de mesure, puis re-mesurer. Sans effet sur l'honnêteté du produit — le lot 2 transforme déjà ces pannes en `NON VÉRIFIÉ`, jamais en faux vert |
| L6-e | **`cited` sous-compte l'usage réel des conventions** : sur `abyss`, la branche Tests a justifié son feu vert en s'appuyant sur une vraie règle du `CLAUDE.md` de MangoOS (« le dépôt n'exige que `tsc --noEmit` + `npm run build` ») — mais en la **paraphrasant** dans son résumé, sans mettre son identifiant dans `conventionRefs`. Le rapport affiche donc `cited: []` alors qu'une convention a manifestement pesé sur le verdict | P2 | 🟡 OUVERT — miroir de l'avertissement du lot 3 (« une règle non citée peut avoir été vérifiée ») : ici une règle non citée a été **utilisée**. Le mécanisme de vérification reste bon (il bloque les citations inventées), mais il ne mesure pas ce qu'on croyait. Piste : rendre la citation obligatoire quand le résumé contient un fragment d'une règle fournie — à peser, le risque étant de pousser le modèle à citer pour citer |
| L6-d | **`LONG-01` affirme `accessibility: 'pass'` alors qu'il n'a jamais été construit comme un contrôle d'accessibilité** — c'est le contrôle négatif des CLÉS DE LISTE. La branche a rendu `fail` en citant un **saut de niveau de titre** (cartes en `h3`, aucun `h2` pour la grille de résultats, le seul `h2` étant dans l'`aside` des favoris). **Fait vérifié à la main : c'est exact.** Ce n'est donc pas une hallucination — c'est le corpus qui a affirmé plus qu'il ne pouvait, exactement comme pour `architecture` sur le même cas le 2026-08-05 | P2 | ✅ **FAIT (2026-08-08, à froid, décision de Raf)** — **la fixture a été réparée, pas l'assertion retirée.** Un `<h2 id="titre-resultats">` a été ajouté avant la grille : les cartes en `h3` suivaient directement le `h1`. Les deux issues étaient défendables, mais retirer l'assertion aurait fait disparaître le **symptôme en gardant la cause** — un contrôle « propre » qui contient un vrai défaut n'est pas un contrôle, et toute branche notée dessus produira des faux positifs légitimes. **Re-mesuré** (famille LONG, 2 passes, Opus 5) : accessibilité passe de **2 FP** à **0/4**, sans rien perdre ailleurs. ⚠️ Le chiffre publié le 2026-08-08 (2/8) reste celui mesuré **avant** la réparation : on ne réécrit pas une mesure passée, on date la correction et on re-mesure |
| L6-c | **Le harnais d'éval avait UN LOT DE RETARD sur le produit qu'il mesure** : depuis le lot 2, `BranchFinding.abstention` distingue un jugement rendu (« pas ma spécialité ») d'une PANNE de l'auditeur (cerveau injoignable, réponse hors contrat). Le harnais fondait les deux dans un seul « abstention ». **Conséquence mesurée** : 3 pannes transitoires du SDK Claude (`Reached maximum number of turns`) ont fait tomber architecture à **3/4** et tests à **1/2** — des scores de JUGEMENT dégradés par des incidents de RÉSEAU. Publier ces chiffres aurait été faux, dans le sens pessimiste cette fois | **P1 — le lot 6 publie ces chiffres** | ✅ FAIT (2026-08-08) — le harnais lit `estPanne()`, **la même fonction que la production**. Une panne sort de tous les dénominateurs de qualité (comme `hors-perimetre`), ne compte plus comme un verdict divergent, et s'affiche dans **sa propre colonne** : la taire flatterait la mesure autant que la fondre dans les abstentions la noircissait. Mesure relancée avec l'instrument corrigé |
| L6-b | **Un test qui ne tourne pas tout seul finit par mentir** : `tests/manual/test-mcp-stdio.ts` affirmait « les 6 branches sont listées » en dur. Le lot 4 en a ajouté une septième — la suite Vitest est restée verte, le test manuel n'a rien dit parce que **personne ne l'exécute**. Trouvé en le lançant à la main, deux lots plus tard | P2 | ✅ FAIT (2026-08-08) — l'attendu est dérivé de `ALL_BRANCHES.length`, comme les descriptions MCP et l'aide de la CLI. **Reste ouvert derrière** : 13 fichiers de `tests/manual/` ne sont dans aucune suite automatique ; ils vérifient des choses qu'un test unitaire ne peut pas voir (protocole stdio réel, vrais appels LLM), donc les automatiser demande un runner à part — pas juste un import |
| L6-a | **L'en-tête du harnais d'éval se trompait sur son propre cerveau** : il lisait `QA_OLLAMA_MODEL` et IGNORAIT `QA_BRAIN`, écrit avant que le cerveau devienne sélectionnable (lot 0) et jamais mis à jour. La première mesure du corpus sous Opus 5 s'est donc annoncée « cerveau `qwen2.5-coder:14b`, repli Claude ACTIF » alors qu'aucun octet n'est parti vers Ollama. **Deuxième occurrence de cette faute exacte** (cf. J2-d, corrigé le 2026-08-05) | **P1 — un chiffre publié sous un mauvais nom de cerveau invaliderait tout le lot 6** | ✅ FAIT (2026-08-08) — le harnais appelle désormais `cerveauPrimaire()`, **la même fonction que la production**, au lieu de deviner d'après une variable d'environnement. Le cerveau réel prouvé en pointant `OLLAMA_URL` sur un port mort : l'audit répond quand même en 9,8 s, sans une seule tentative de repli. Mesure relancée avec le harnais corrigé |
| L5-b | **`--spec <issue>` : seul le chemin d'ERREUR est prouvé.** `gh` est installé sur cette machine mais **pas authentifié** (`gh auth login` jamais fait) : la lecture réussie d'une vraie issue n'a jamais tourné de bout en bout. Ce qui EST vérifié, c'est la dégradation honnête — `mangoqa . --spec "#999999"` rend code **2**, aucun audit lancé, et relaie le message de `gh` lui-même (« To get started with GitHub CLI, please run: gh auth login ») avec notre propre piste de résolution | P3 | 🟡 OUVERT — une commande `gh auth login` et un rejeu suffisent. Même famille que L139/L140/L141 : le mécanisme est là, la preuve du chemin nominal manque. Consigné plutôt que présenté comme acquis |
| L5-a | **`mangoqa init` n'écrivait que dans `process.cwd()`** : lancé depuis un autre dossier que celui qu'on croit équiper, il pose `.mcp.json`, le hook et le workflow dans le mauvais dépôt. Arrivé en réel pendant la vérification du lot 5 — victime : le dépôt de Mango QA lui-même, dégât nul, mais chez un utilisateur c'est un `.mcp.json` inattendu dans un projet qui n'a rien demandé | P2 | ✅ FAIT (2026-08-08) — argument de dossier facultatif (`mangoqa init ./mon-projet`), et le chemin visé **annoncé en tête de sortie**. Toutes les autres commandes de cette CLI prennent un dossier ; celle-ci n'avait aucune raison de faire exception. Gelé par 2 tests |
| L4-b | **La valeur de l'axe Spec est mesurée sur des specs que j'ai écrites moi-même**, en puces courtes et nettes, à partir du texte de l'ADR. C'est bien la demande réelle, mais pas le régime normal en entreprise : un vrai ticket est bavard, ambigu, à moitié périmé, mélange contexte et exigences | P2 | 🟡 OUVERT — même famille que L3-b : le mécanisme est prouvé (contrôle apparié réel : vert sur la spec implémentée, rouge cité sur celle qui ne l'est pas), sa robustesse au bruit ne l'est pas. À mesurer au lot 6 sur de vrais tickets |
| L3-a | **Le rendu du rapport était recopié dans `eval/audit-projet.ts`**, et la copie avait divergé : la section CONVENTIONS ajoutée à `cli.ts` et `mcp.ts` n'y figurait pas. **Deuxième occurrence de la même classe de défaut en deux lots** (au lot 2, c'était la règle de dégradation recopiée dans trois surfaces) — trouvé par Mango QA sur son propre diff les deux fois | P2 | ✅ FAIT (2026-08-08) — `audit-projet.ts` importe `rendreRapport` et ne garde que son en-tête de conditions de mesure et son journal. **Tendance à surveiller** : le réflexe « je duplique pour ne pas toucher à l'existant » produit une divergence à chaque ajout de section |
| L3-b | **La VALEUR de l'axe *Standards* n'est pas mesurée** : aucun dépôt de cette machine ne documente de vraies conventions **de code** (celles de MangoOS sont des instructions d'agent — heartbeats, lecture de `statut.md`). Le mécanisme est prouvé de bout en bout sur un dépôt temporaire réel avec cerveau injecté ; jamais un vrai modèle citant une vraie règle de code sur un vrai dépôt | P2 | 🟡 OUVERT — à mesurer au lot 6 sur un dépôt tiers qui écrit ses règles de code. Résultat encourageant en attendant : **34 règles hors sujet fournies, zéro citation abusive** — la retenue tient sous la charge la plus propice à l'invention |
| L2-b | `projectHasTests` émet `ENOENT … scandir '<dossier>/src'` sur stderr quand le dossier audité **est** lui-même un `src/`. Aucun effet sur le verdict ni sur la couverture — mais un auditeur qui crache une erreur système pendant qu'il rassure sur la qualité envoie un signal contradictoire | P3 | 🟡 OUVERT — tester l'existence avant de parcourir |
| J1-d | Intégration MangoOS : un audit local sur projet réel dure ~365 s contre un budget `MANGOQA_CLOSURE_TIMEOUT` de 60 s → chaque clôture serait marquée « non-vérifiée » (fail-open) et l'audit disparaîtrait. `surfaceVerdict` (voie 300 s) n'est appelée nulle part | P2 | 🟡 OUVERT — à trancher **côté MangoOS**, sans effet sur l'usage autonome (CLI/CI/MCP) |
| J2-b | `flux-eye/deep.ts` gardait son propre cap (20 000) et rendait un payload **sans couverture** — même classe de défaut que J1-a, sur un autre visage | P2 | ✅ FAIT (2026-08-05) — couverture mesurée, déclarée au modèle, portée dans `FluxDeepObservation.coverage` ET recopiée dans le `summary` écrit en JSON. Cap **laissé à 20 000** : ce visage n'a jamais été mesuré contre un corpus, le relever sans donnée serait refaire l'erreur en sens inverse. Réglable par `FLUX_DEEP_PAYLOAD_CAP` |
| J2-c | **Le harnais de mesure était complaisant** : sur un cas propre, `relevant() === []` était compté `correct-pass`, donc au CRÉDIT de la branche. Security affichait « 0 faux positif / 6 » pour **3 jugements réels** — dénominateur gonflé de non-événements, dans le sens flatteur. C'est J1-a déplacé dans l'instrument : confondre « n'a pas regardé » et « a regardé et n'a rien trouvé » | P1 | ✅ FAIT (2026-08-05) — catégorie `hors-perimetre` distincte, exclue du dénominateur et affichée à côté |
| J3-a | **Détection du point d'entrée par le NOM du fichier** : `/cli\.(ts\|js)$/` sur `process.argv[1]`. `npm i -g` installe un lien nommé `mangoqa` — le motif ne correspond plus. La commande se serait terminée **en silence, code 0, sans rien auditer**. Invisible en développement, fatal à l'installation, et le pire mode de panne pour un outil dont la promesse est de ne pas se taire | P1 | ✅ FAIT (2026-08-05) — comparaison d'URL de module sur le chemin réel ; valide sous `tsx`, sous `node dist/` et en binaire installé. Gelé par `test-packaging.ts` |
| J3-b | **355 Mo installés pour une CLI de 1,5 Mo** : le SDK Claude (280 Mo) est le cerveau de **repli**, les grammaires WASM (50 Mo) servent un visage que la CLI n'appelle jamais. Contredit frontalement l'argument de souveraineté du produit. Aggravé par `auditProject` important ses 3 fonctions de lecture via `orchestrator.ts`, ce qui tirait tout le graphe des visages | P1 | ✅ FAIT (2026-08-05) — lecture extraite dans `project-files.ts` (déplacement pur, `orchestrator` ré-exporte) ; lourds passés en dépendances de pair **optionnelles** + imports dynamiques + messages actionnables. **355 Mo → 1,6 Mo**, serveur MCP compris |
| J2-d | En-tête des rapports d'éval **faux sur ses propres conditions** : il n'annonçait « repli Claude COUPÉ » que si `--brain` avait été passé, donc un `QA_LOCAL_ONLY=on` posé par l'environnement produisait un rapport affirmant qu'un repli cloud était armé. Plus l'horodatage en UTC (nom de fichier ET en-tête), déjà corrigé dans `_ceiling.ts` mais pas ici | P2 | ✅ FAIT (2026-08-05) — conditions lues dans l'environnement, heure locale partout |

## Notes

- **Q1/Q5** : le pattern anti-OOM (#L70) prouvé par `readBusEvents` est désormais mutualisé dans `src/jsonl.ts` (`readJsonlTail` stateless, `readJsonlSince` incrémental). Toute nouvelle lecture de `.jsonl` doit passer par ce module.
- **Q2** : `SUITE_EYE=on` active l'Auditeur de Suite dans le cycle de phase (défaut off = comportement historique). `run-suite-eye.ts` reste utilisable en CLI ponctuel.
- **Q3** : règle gravée — un `catch` fail-open sans trace est interdit ; seul ENOENT (fichier pas encore créé = état normal) reste silencieux.
- **R1** : la fusion par-fichier (au lieu du blob concaténé) peut en théorie diverger du blob dans un cas adversarial (un fichier gravement malformé qui aurait corrompu tout le parse du blob) ; sur du code réel (top-level statements indépendants), le comportement observable est identique — prouvé par la suite `test-flux-eye.ts` (résolution de constantes réparties sur 3+ fichiers tiers, setter appelé depuis 4 fichiers distincts, redéfinition de clé, fichier cassé à côté de fichiers sains).
- **R3** : `walkTree` généralise `walk`/`walkSrc` via un callback `visit` — reproduit à l'identique la nuance historique où un fichier illisible ne consommait pas de place dans `maxFiles` (flux-eye) alors qu'un chemin non lu en comptait un (orchestrator, qui ne lit pas le contenu à ce stade).
- **Visage 2 (Observateur-Conseil)** : amorce non câblée — `src/observer.ts` (module pur, événements injectés) + `src/test-observer.ts`. Détecte des patterns de défaillance récurrents (branche/règle/projet au-dessus d'un seuil configurable) et produit des SUGGESTIONS texte pour Raf, jamais d'action. Volontairement PAS branché dans `orchestrator.ts`/`index.ts` — à câbler dans une session dédiée (source d'événements réelle à définir : Retex existant ou nouvel historique de verdicts).
