> ⚠️ **RAPPORT PÉRIMÉ — conservé comme trace de mesure, pas comme référence.**
> Deux défauts du harnais, corrigés depuis (cf. `FAILLES.md` J2-c/J2-d), l'affectent :
> l'en-tête ci-dessous annonce un « repli `QA_MODEL=haiku` » alors que `QA_LOCAL_ONLY=on`
> l'avait COUPÉ (aucune réponse ne vient de Claude), et l'horodatage est en UTC (il était
> 02:05 en heure locale). Mesure de référence : `J2-COUVERTURE.md`.

# Mango QA — J0 · Mesure de la qualité de jugement

> Exécuté le 2026-08-05T00:05:28.726Z · corpus : **27 cas** (21 à défaut · 6 propres)
> Passes : **1** · cerveau : `QA_OLLAMA_MODEL=qwen2.5-coder:14b` → repli `QA_MODEL=haiku`
> Durée totale : 439.5 s · observations : 9

## Résultat par branche

| Branche | Détection | Ratés | Non pertinents | Faux positifs | Abstentions | Instables | Durée moy. |
|---|---|---|---|---|---|---|---|
| architecture | **0/0** (—) | 0 | 0 | **1/2** | 0 | — | 50.5 s |
| security | **1/1** (100 %) | 0 | 0 | **0/2** | 0 | — | 39.6 s |
| accessibility | **0/0** (—) | 0 | 0 | **0/1** | 0 | — | 33.1 s |
| performance | **1/1** (100 %) | 0 | 0 | **0/2** | 0 | — | 62.1 s |

## Détail des 1 observation(s) à revoir

### ⚠️ FAUX POSITIF · `LONG-01-catalogue-propre` → branche `architecture`

- **Défaut injecté** : RIEN — catalogue React de ~300 lignes, TOUTES les listes ont une clé stable (reproduction du faux positif J1 : `key={c.id}` était présent, la branche a exigé de l'ajouter).
- **Emplacement** : `src/components/Catalogue.jsx — 4 .map(), 4 clés`
- **Règle attendue** : React — clés de liste (contrôle négatif)
- **Attendu** : `pass` · **Obtenu** : `fail`
- **Ce qu'a répondu la branche** : Le composant Catalogue.jsx est un fichier monolithe avec plus de 300 lignes, ce qui rend la maintenance coûteuse.

## Comment lire ces chiffres

- **Détection** : un cas ne compte que si TOUTES les passes l'ont détecté (exigeant volontairement).
- **Non pertinent** : `relevant()` n'a retenu aucun fichier — la branche n'a même pas regardé.
  C'est un trou de **filtrage**, pas de jugement : aucun modèle, si bon soit-il, ne peut le rattraper.
- **Abstention** : la branche a répondu `skip` (réponse illisible, erreur interne). Ni détection, ni faute.
- **Instables** : verdicts différents d'une passe à l'autre sur le même cas. Un auditeur instable est
  inutilisable en CI, même avec un bon taux de détection.
- **Un score de détection élevé n'a de sens qu'accompagné d'un taux de faux positifs bas.** Une branche
  qui répondrait `fail` à tout obtiendrait 100 % de détection et serait sans valeur.