# Mango QA — J0 · Mesure de la qualité de jugement

> Exécuté le 2026-08-04T20:03:47.577Z · corpus : **23 cas** (19 à défaut · 4 propres)
> Passes : **3** · cerveau : **`gemma4:latest`** — repli Claude COUPÉ (`QA_LOCAL_ONLY=on`), timeout 300 s
> Durée totale : 1420.8 s · observations : 93

## Résultat par branche

| Branche | Détection | Ratés | Non pertinents | Faux positifs | Abstentions | Instables | Durée moy. |
|---|---|---|---|---|---|---|---|
| architecture | **3/3** (100 %) | 0 | 0 | **4/12** | 0 | 1 | 16.4 s |
| security | **4/5** (80 %) | 0 | 0 | **0/6** | 1 | 1 | 15.0 s |
| accessibility | **3/4** (75 %) | 1 | 0 | **0/9** | 0 | 1 | 13.1 s |
| performance | **3/3** (100 %) | 0 | 0 | **0/9** | 2 | 1 | 16.7 s |
| tests | **1/1** (100 %) | 0 | 0 | **3/3** | 0 | 0 | 15.4 s |
| design-system | **0/0** (—) | 0 | 0 | **0/6** | 0 | 0 | 15.7 s |

## Branches de conseil (ne peuvent jamais bloquer)

| Branche | Défaut mentionné dans le résumé |
|---|---|
| design-system | **6/6** (100 %) |

## Détail des 8 observation(s) à revoir

### ⚠️ FAUX POSITIF · `TEST-02-controle-logique-testee` → branche `tests` (passe 1)

- **Défaut injecté** : CONTRÔLE POSITIF — même logique métier, avec ses tests. Ne doit PAS être rejetée.
- **Attendu** : `pass` · **Obtenu** : `fail`
- **Ce qu'a répondu la branche** : La logique de calcul des remises est présente mais les tests ne couvrent pas suffisamment les zones de transition entre les paliers.

### ⚠️ FAUX POSITIF · `CLEAN-02-route-api-sure` → branche `architecture` (passe 1)

- **Défaut injecté** : RIEN — route Express avec requête paramétrée, CORS restreint, chemin validé.
- **Attendu** : `pass` · **Obtenu** : `fail`
- **Ce qu'a répondu la branche** : La logique de recherche et d'accès aux données est directement imbriquée dans le gestionnaire de route, violant la séparation des responsabilités.

### ⚠️ FAUX POSITIF · `CLEAN-04-liste-paginee` → branche `architecture` (passe 1)

- **Défaut injecté** : RIEN — liste paginée côté serveur, dépendances explicites, imports ciblés.
- **Attendu** : `pass` · **Obtenu** : `fail`
- **Ce qu'a répondu la branche** : Le composant mélange la logique de récupération des données (API, pagination) et le rendu UI, ce qui augmente le couplage entre les couches présentation et data.

### ❌ RATÉ · `A11Y-01-contraste` → branche `accessibility` (passe 2)

- **Défaut injecté** : Texte #9a9a9a sur fond #ffffff → ratio ≈ 2.8:1 (seuil AA : 4.5:1).
- **Emplacement** : `src/components/Nav.tsx:6`
- **Règle attendue** : WCAG 1.4.3 — Contrast (Minimum)
- **Attendu** : `fail` · **Obtenu** : `pass`
- **Ce qu'a répondu la branche** : Le composant de navigation est sémantiquement correct et utilise des liens <a> appropriés pour la navigation.

### ⚠️ FAUX POSITIF · `TEST-02-controle-logique-testee` → branche `tests` (passe 2)

- **Défaut injecté** : CONTRÔLE POSITIF — même logique métier, avec ses tests. Ne doit PAS être rejetée.
- **Attendu** : `pass` · **Obtenu** : `fail`
- **Ce qu'a répondu la branche** : La logique de remise est bien testée aux bornes des paliers, mais manque de couverture pour les valeurs intermédiaires entre ces seuils.

### ⚠️ FAUX POSITIF · `CLEAN-04-liste-paginee` → branche `architecture` (passe 2)

- **Défaut injecté** : RIEN — liste paginée côté serveur, dépendances explicites, imports ciblés.
- **Attendu** : `pass` · **Obtenu** : `fail`
- **Ce qu'a répondu la branche** : Le composant mélange la logique de gestion d'état et le cycle de vie des effets secondaires (fetch API) avec le rendu, ce qui rend les tests unitaires difficiles à isoler.

### ⚠️ FAUX POSITIF · `TEST-02-controle-logique-testee` → branche `tests` (passe 3)

- **Défaut injecté** : CONTRÔLE POSITIF — même logique métier, avec ses tests. Ne doit PAS être rejetée.
- **Attendu** : `pass` · **Obtenu** : `fail`
- **Ce qu'a répondu la branche** : La logique de remise est bien testée aux bornes des paliers, mais manque de couverture pour les valeurs intermédiaires entre ces seuils.

### ⚠️ FAUX POSITIF · `CLEAN-04-liste-paginee` → branche `architecture` (passe 3)

- **Défaut injecté** : RIEN — liste paginée côté serveur, dépendances explicites, imports ciblés.
- **Attendu** : `pass` · **Obtenu** : `fail`
- **Ce qu'a répondu la branche** : Le composant mélange la logique de récupération des données (side effect), la gestion d'état et le rendu, ce qui viole la séparation des responsabilités et rend difficile la réutilisation du mécanisme de fetching.

## Comment lire ces chiffres

- **Détection** : un cas ne compte que si TOUTES les passes l'ont détecté (exigeant volontairement).
- **Non pertinent** : `relevant()` n'a retenu aucun fichier — la branche n'a même pas regardé.
  C'est un trou de **filtrage**, pas de jugement : aucun modèle, si bon soit-il, ne peut le rattraper.
- **Abstention** : la branche a répondu `skip` (réponse illisible, erreur interne). Ni détection, ni faute.
- **Instables** : verdicts différents d'une passe à l'autre sur le même cas. Un auditeur instable est
  inutilisable en CI, même avec un bon taux de détection.
- **Un score de détection élevé n'a de sens qu'accompagné d'un taux de faux positifs bas.** Une branche
  qui répondrait `fail` à tout obtiendrait 100 % de détection et serait sans valeur.