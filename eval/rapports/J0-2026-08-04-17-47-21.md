# Mango QA — J0 · Mesure de la qualité de jugement

> Exécuté le 2026-08-04T17:47:21.241Z · corpus : **23 cas** (19 à défaut · 4 propres)
> Passes : **1** · cerveau : `QA_OLLAMA_MODEL=(non défini)` → repli `QA_MODEL=haiku`
> Durée totale : 1991.8 s · observations : 31

## Résultat par branche

| Branche | Détection | Ratés | Non pertinents | Faux positifs | Abstentions | Instables | Durée moy. |
|---|---|---|---|---|---|---|---|
| architecture | **3/3** (100 %) | 0 | 0 | **1/4** | 0 | — | 89.5 s |
| security | **4/5** (80 %) | 0 | 1 | **0/2** | 0 | — | 35.4 s |
| accessibility | **4/4** (100 %) | 0 | 0 | **0/3** | 0 | — | 62.8 s |
| performance | **2/3** (67 %) | 0 | 1 | **0/3** | 1 | — | 64.0 s |
| tests | **1/1** (100 %) | 0 | 0 | **0/1** | 0 | — | 57.1 s |
| design-system | **0/0** (—) | 0 | 0 | **0/2** | 0 | — | 89.7 s |

## Branches de conseil (ne peuvent jamais bloquer)

| Branche | Défaut mentionné dans le résumé |
|---|---|
| design-system | **2/2** (100 %) |

## Détail des 3 observation(s) à revoir

### 🚫 NON PERTINENT (filtre) · `SEC-01-cle-service-exposee` → branche `security`

- **Défaut injecté** : Clé de SERVICE Supabase préfixée VITE_ → inlinée dans le bundle client.
- **Emplacement** : `src/lib/db.ts:4`
- **Règle attendue** : OWASP A05 — Security Misconfiguration
- **Attendu** : `fail` · **Obtenu** : `non-pertinent`
- **Ce qu'a répondu la branche** : Aucun fichier retenu par relevant().

### 🚫 NON PERTINENT (filtre) · `PERF-03-import-lourd` → branche `performance`

- **Défaut injecté** : Import de lodash entier et de moment pour une seule fonction chacun.
- **Emplacement** : `src/utils/format.ts:1`
- **Attendu** : `fail` · **Obtenu** : `non-pertinent`
- **Ce qu'a répondu la branche** : Aucun fichier retenu par relevant().

### ⚠️ FAUX POSITIF · `CLEAN-04-liste-paginee` → branche `architecture`

- **Défaut injecté** : RIEN — liste paginée côté serveur, dépendances explicites, imports ciblés.
- **Attendu** : `pass` · **Obtenu** : `fail`
- **Ce qu'a répondu la branche** : Composant monolithe mélange data-fetching, état et présentation dans une même responsabilité.

## Comment lire ces chiffres

- **Détection** : un cas ne compte que si TOUTES les passes l'ont détecté (exigeant volontairement).
- **Non pertinent** : `relevant()` n'a retenu aucun fichier — la branche n'a même pas regardé.
  C'est un trou de **filtrage**, pas de jugement : aucun modèle, si bon soit-il, ne peut le rattraper.
- **Abstention** : la branche a répondu `skip` (réponse illisible, erreur interne). Ni détection, ni faute.
- **Instables** : verdicts différents d'une passe à l'autre sur le même cas. Un auditeur instable est
  inutilisable en CI, même avec un bon taux de détection.
- **Un score de détection élevé n'a de sens qu'accompagné d'un taux de faux positifs bas.** Une branche
  qui répondrait `fail` à tout obtiendrait 100 % de détection et serait sans valeur.