# Mango QA — J0 · Mesure de la qualité de jugement

> Exécuté le 2026-08-08 04:06 (heure locale) · corpus : **32 cas** (24 à défaut · 8 propres)
> Passes : **1** · cerveau : **`qwen2.5-coder:14b`** · timeout 180 s · repli Claude **ACTIF** vers `claude-opus-5` ⚠️ une réponse peut ne PAS venir du modèle mesuré
> Durée totale : 421.3 s · observations : 44

## Résultat par branche

| Branche | Détection | Ratés | Non pertinents | Faux positifs | Abstentions | Instables | Durée moy. |
|---|---|---|---|---|---|---|---|
| architecture | **4/4** (100 %) | 0 | 0 | **0/5** | 0 | — | 8.3 s |
| security | **6/6** (100 %) | 0 | 0 | **0/2** (+2 hors périmètre) | 0 | — | 7.4 s |
| accessibility | **5/5** (100 %) | 0 | 0 | **2/4** (+1 hors périmètre) | 0 | — | 13.9 s |
| performance | **4/4** (100 %) | 0 | 0 | **0/5** | 2 | — | 8.5 s |
| tests | **2/2** (100 %) | 0 | 0 | **0/2** | 0 | — | 9.8 s |
| design-system | **0/0** (—) | 0 | 0 | **0/2** | 0 | — | 8.7 s |

## Branches de conseil (ne peuvent jamais bloquer)

| Branche | Défaut mentionné dans le résumé |
|---|---|
| design-system | **2/2** (100 %) |

## Détail des 2 observation(s) à revoir

### ⚠️ FAUX POSITIF · `LONG-01-catalogue-propre` → branche `accessibility`

- **Défaut injecté** : RIEN côté clés de liste — catalogue React de 210 lignes, TOUTES les listes ont une clé stable (reproduction du faux positif J1 : `key={c.id}` était présent, la branche a exigé de l'ajouter).
- **Emplacement** : `src/components/Catalogue.jsx — 4 .map(), 4 clés`
- **Règle attendue** : React — clés de liste (contrôle négatif)
- **Attendu** : `pass` · **Obtenu** : `fail`
- **Ce qu'a répondu la branche** : La hiérarchie des titres saute du h1 du catalogue au h3 des cartes, sans niveau h2 intermédiaire.

### ⚠️ FAUX POSITIF · `LONG-08-parametres-propre` → branche `accessibility`

- **Défaut injecté** : RIEN — page de paramètres de ~200 lignes, accessible de bout en bout : chaque champ a son <label htmlFor>, les groupes ont fieldset/legend, l'avatar a un alt utile, les messages passent par une région aria-live, et toutes les commandes sont de vrais <button>.
- **Emplacement** : `src/pages/ParametresCompte.jsx`
- **Règle attendue** : Contrôle négatif long (WCAG 2.2)
- **Attendu** : `pass` · **Obtenu** : `fail`
- **Ce qu'a répondu la branche** : Le champ courriel est marqué aria-invalid="true" sans qu'aucun message d'erreur textuel ne décrive le problème, alors que le bouton d'envoi est simultanément désactivé.

## Comment lire ces chiffres

- **Détection** : un cas ne compte que si TOUTES les passes l'ont détecté (exigeant volontairement).
- **⚠️ Deux unités dans ce tableau.** « Détection » se compte en **cas** (dénominateur = nombre de cas
  à défaut), « Faux positifs » et « Abstentions » en **observations** (cas × passes). `1/1` en détection
  sur 3 passes veut donc dire « l'unique cas à défaut, détecté aux 3 passes », pas « une passe sur une ».
- **Non pertinent** : sur un cas à DÉFAUT, `relevant()` n'a retenu aucun fichier — la branche
  n'a même pas regardé. C'est un trou de **filtrage**, pas de jugement : aucun modèle, si bon
  soit-il, ne peut le rattraper.
- **Hors périmètre** : sur un cas PROPRE, `relevant()` n'a rien retenu non plus. Ce n'est ni un
  mérite ni une faute, c'est un non-événement — donc **exclu du dénominateur des faux positifs**.
  Le compter en « pass correct » gonflait le dénominateur de jugements qui n'ont jamais eu lieu,
  et flattait le taux (corrigé le 2026-08-05 : security affichait 0/6 pour 3 jugements réels).
- **Abstention** : la branche a répondu `skip` (réponse illisible, erreur interne). Ni détection, ni faute.
- **Instables** : verdicts différents d'une passe à l'autre sur le même cas. Un auditeur instable est
  inutilisable en CI, même avec un bon taux de détection.
- **Un score de détection élevé n'a de sens qu'accompagné d'un taux de faux positifs bas.** Une branche
  qui répondrait `fail` à tout obtiendrait 100 % de détection et serait sans valeur.